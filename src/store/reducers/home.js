const initialState = {};

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default: {
      break;
    }
  }
  return state;
};

export {
  reducer as default, //
  initialState as homeInitialState,
};
