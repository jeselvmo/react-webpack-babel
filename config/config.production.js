// 生产环境
const config = {
  production: true,
  // ////////////////////////////////////////////////////////////////
  // 接口 baseURL
  baseURL: 'https://cs.ljlx.com/api/',
  // 发布路径
  publicPath: '/items/mooc/',
};

module.exports = config;
